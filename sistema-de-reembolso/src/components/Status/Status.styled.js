import styled from "styled-components";
import { primaryColor } from "../../utils/colors";

export const RefundSituation = styled.select`
    width: 210px;
    padding: 4px;
    border: 2px solid ${primaryColor};
    border-radius: 4px;
`
